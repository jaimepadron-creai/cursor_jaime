/**
 * Página de login (Módulo 9). Envuelve LoginForm para la ruta /login.
 */
import React from 'react'
import { LoginForm } from '../components/LoginForm/LoginForm'

export function LoginPage() {
  return <LoginForm />
}

export default LoginPage
